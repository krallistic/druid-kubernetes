apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.nodes.localZk.service.name }}
  #TODO flexible service Name, configMap
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
spec:
  type: {{ .Values.nodes.localZk.service.type }}
  ports:
  {{range .Values.nodes.localZk.service.ports }}
  - name: {{ .name }}
    port: {{ .port }}
  {{end}}
  selector:
    name: "{{ .Chart.Name }}-{{ .Values.nodes.localZk.name }}-rc"