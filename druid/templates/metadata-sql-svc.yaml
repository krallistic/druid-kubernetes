apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.nodes.metadataSql.service.name }}
  #TODO flexible service Name, configMap
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
spec:
  type: {{ .Values.nodes.metadataSql.service.type }}
  ports:
  {{range .Values.nodes.metadataSql.service.ports }}
  - name: {{ .name }}
    port: {{ .port }}
  {{end}}
  selector:
    name: "{{ .Chart.Name }}-{{ .Values.nodes.metadataSql.name }}-rc"